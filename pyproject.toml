[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "eph-extractor"
version = "1.0.0"
description = "Bounded, provenance-preserving EPH acquisition"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
eph-extractor = "eph_extractor.cli:main"

[tool.setuptools.package-data]
eph_extractor = ["settings.yaml"]
